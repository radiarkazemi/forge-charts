# MT5 WatchBridge

Shared include used by TRH / ICT / CRT AutoTrade EAs to push events to the local **mt5-watch** webapp.

- Master: `WatchBridge.mqh` (this folder)
- Also copied beside each AutoTrade for compile

See `mt5-watch/README.md` for install steps.
