# Disabled: Yahoo GC=F alerts were false vs FOREXCOM chart and used old format.
# Phone alerts come only from VPS trh-mongo-alert (FOREXCOM Mongo).
#
# To re-enable temporarily, rename back to trh-alerts.yml — not recommended.

name: TRH Gold Alerts (DISABLED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Yahoo/GitHub TRH alerts are DISABLED."
          echo "Use VPS trh-alert (FOREXCOM) + Android app only."
          exit 0
